import React from 'react';
import { Store } from '../../types';
import {\n  MousePointer,\n  Package,\n  Ruler,\n  Eye,\n  TrendingUp,\n  CheckCircle,\n  AlertTriangle\n} from 'lucide-react';\n\ninterface ToolbarPanelProps {\n  mode: 'select' | 'place' | 'measure';\n  onModeChange: (mode: 'select' | 'place' | 'measure') => void;\n  store: Store;\n}\n\nconst ToolbarPanel: React.FC<ToolbarPanelProps> = ({\n  mode,\n  onModeChange,\n  store\n}) => {\n  const tools = [\n    {\n      id: 'select' as const,\n      icon: MousePointer,\n      label: 'Select',\n      description: 'Select and modify fixtures'\n    },\n    {\n      id: 'place' as const,\n      icon: Package,\n      label: 'Place',\n      description: 'Place new fixtures'\n    },\n    {\n      id: 'measure' as const,\n      icon: Ruler,\n      label: 'Measure',\n      description: 'Measure distances'\n    }\n  ];\n\n  return (\n    <div className=\"bg-white border-b border-gray-200 px-6 py-3\">\n      <div className=\"flex items-center justify-between\">\n        {/* Tool Selection */}\n        <div className=\"flex items-center space-x-1 bg-gray-100 rounded-lg p-1\">\n          {tools.map((tool) => {\n            const Icon = tool.icon;\n            return (\n              <button\n                key={tool.id}\n                onClick={() => onModeChange(tool.id)}\n                className={`\n                  flex items-center px-3 py-2 rounded text-sm font-medium transition-colors\n                  ${\n                    mode === tool.id\n                      ? 'bg-white text-primary shadow-sm'\n                      : 'text-gray-600 hover:text-gray-900'\n                  }\n                `}\n                title={tool.description}\n              >\n                <Icon className=\"w-4 h-4 mr-2\" />\n                {tool.label}\n              </button>\n            );\n          })}\n        </div>\n\n        {/* Store Info & Analytics */}\n        <div className=\"flex items-center space-x-6 text-sm\">\n          <div className=\"flex items-center text-gray-600\">\n            <Ruler className=\"w-4 h-4 mr-1\" />\n            <span>{store.dimensions.width}' × {store.dimensions.height}'</span>\n          </div>\n\n          <div className=\"flex items-center text-gray-600\">\n            <Package className=\"w-4 h-4 mr-1\" />\n            <span>0 Fixtures</span>\n          </div>\n\n          <div className=\"flex items-center space-x-3\">\n            <div className=\"flex items-center text-green-600\">\n              <CheckCircle className=\"w-4 h-4 mr-1\" />\n              <span className=\"text-xs\">VM Compliance: 85%</span>\n            </div>\n\n            <div className=\"flex items-center text-blue-600\">\n              <TrendingUp className=\"w-4 h-4 mr-1\" />\n              <span className=\"text-xs\">Traffic Flow: Good</span>\n            </div>\n\n            <div className=\"flex items-center text-orange-600\">\n              <Eye className=\"w-4 h-4 mr-1\" />\n              <span className=\"text-xs\">Sight Lines: Review</span>\n            </div>\n          </div>\n        </div>\n      </div>\n    </div>\n  );\n};\n\nexport default ToolbarPanel;